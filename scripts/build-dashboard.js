const fs = require('node:fs');
const path = require('node:path');

const root = path.resolve(__dirname, '..');
const source = JSON.parse(fs.readFileSync(path.join(root, 'brain_deliverables.json'), 'utf8'));
const safeJson = JSON.stringify(source).replace(/</g, '\\u003c');
const outputDir = path.join(root, 'output');
fs.mkdirSync(outputDir, { recursive: true });

const html = String.raw`<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>FitLife Brands · Crawl archive</title>
<style>
:root{--bg:#0b1020;--panel:#131b2f;--panel2:#18233b;--ink:#edf3ff;--muted:#9eacc7;--line:#2b3b5e;--accent:#6ee7c8;--accent2:#8ab4ff;--warn:#ffc56e;--bad:#ff8f9e;--shadow:0 18px 50px #03061155}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 10% 0,#1b2e52 0,#0b1020 42%);color:var(--ink);font:14px/1.55 Inter,ui-sans-serif,system-ui,-apple-system,Segoe UI,sans-serif}button,input,select{font:inherit}button{cursor:pointer}.app{display:grid;grid-template-columns:270px 1fr;min-height:100vh}.sidebar{border-right:1px solid var(--line);padding:26px 18px;position:sticky;top:0;height:100vh;background:#0b1020dd;backdrop-filter:blur(14px)}.brand{padding:0 10px 24px;border-bottom:1px solid var(--line);margin-bottom:18px}.eyebrow{color:var(--accent);font-size:11px;letter-spacing:.14em;text-transform:uppercase;font-weight:800}.brand h1{font-size:22px;line-height:1.1;margin:9px 0}.brand p{color:var(--muted);margin:0}.nav{display:grid;gap:7px}.nav button{background:transparent;border:1px solid transparent;color:var(--muted);text-align:left;padding:10px 12px;border-radius:10px}.nav button:hover,.nav button.active{background:var(--panel2);border-color:var(--line);color:var(--ink)}.sidebar-foot{position:absolute;bottom:18px;color:var(--muted);font-size:12px;padding:0 10px}.main{min-width:0;padding:28px clamp(18px,4vw,56px) 64px}.topbar{display:flex;justify-content:space-between;gap:16px;align-items:flex-start;margin-bottom:26px}.topbar h2{margin:3px 0 5px;font-size:30px;letter-spacing:-.03em}.topbar p{margin:0;color:var(--muted)}.search{width:min(430px,100%);background:var(--panel);border:1px solid var(--line);border-radius:12px;color:var(--ink);padding:11px 14px;outline:none}.search:focus{border-color:var(--accent)}.section{display:none}.section.active{display:block}.hero,.card{background:linear-gradient(145deg,#18233bdd,#11192ddd);border:1px solid var(--line);border-radius:16px;box-shadow:var(--shadow)}.hero{padding:28px;margin-bottom:18px}.hero h3{font-size:25px;margin:7px 0}.hero p{color:var(--muted);max-width:900px}.metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin:18px 0}.metric{padding:17px;border:1px solid var(--line);border-radius:13px;background:#10182b}.metric strong{display:block;font-size:25px;color:var(--accent)}.metric span{color:var(--muted);font-size:12px}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px}.card{padding:18px}.card h4{margin:0 0 13px;font-size:15px}.card h4 small{float:right;color:var(--muted);font-weight:400}.chips{display:flex;flex-wrap:wrap;gap:7px}.chip,.status{display:inline-flex;align-items:center;border:1px solid var(--line);border-radius:999px;padding:4px 9px;color:var(--muted);font-size:12px}.status.complete{color:var(--accent);border-color:#2d7867}.status.skipped{color:var(--warn);border-color:#80612a}.status.failed{color:var(--bad);border-color:#813443}.table-wrap{overflow:auto;border:1px solid var(--line);border-radius:12px}.table{width:100%;border-collapse:collapse;min-width:650px}.table th,.table td{border-bottom:1px solid var(--line);padding:10px;text-align:left;vertical-align:top}.table th{color:var(--muted);font-size:11px;text-transform:uppercase;letter-spacing:.08em;background:#10182b}.table tr:last-child td{border-bottom:0}.link-button{background:none;border:0;color:var(--accent2);padding:0;text-align:left}.muted{color:var(--muted)}.warning{border-left:3px solid var(--warn);padding:10px 12px;background:#3b2d1455;color:#ffe1a6;border-radius:5px}.danger{border-left:3px solid var(--bad);padding:10px 12px;background:#3b172455;color:#ffc2ca;border-radius:5px}.stack{display:grid;gap:12px}.toolbar{display:flex;gap:10px;flex-wrap:wrap;margin:14px 0}.toolbar input,.toolbar select{background:var(--panel);border:1px solid var(--line);border-radius:9px;padding:9px 10px;color:var(--ink)}.doc-list{display:grid;gap:9px}.doc{padding:13px;border:1px solid var(--line);border-radius:11px;background:#10182b;display:flex;justify-content:space-between;gap:15px;align-items:center}.doc strong{display:block;overflow-wrap:anywhere}.doc small{color:var(--muted)}.doc button{border:1px solid var(--line);color:var(--accent);background:transparent;border-radius:8px;padding:7px 10px;white-space:nowrap}.detail{margin-top:16px}.detail pre,.raw{white-space:pre-wrap;overflow:auto;max-height:570px;background:#0a0f1d;border:1px solid var(--line);border-radius:10px;padding:15px;color:#dce7ff;font:12px/1.6 ui-monospace,SFMono-Regular,Consolas,monospace}.detail table{width:100%;border-collapse:collapse}.detail td,.detail th{padding:7px;border:1px solid var(--line);text-align:left}.detail h1,.detail h2,.detail h3{color:var(--accent)}.empty{padding:28px;text-align:center;color:var(--muted);border:1px dashed var(--line);border-radius:12px}.footer-note{color:var(--muted);font-size:12px;margin-top:16px}@media(max-width:900px){.app{grid-template-columns:1fr}.sidebar{position:static;height:auto;border-right:0;border-bottom:1px solid var(--line)}.sidebar-foot{position:static;margin-top:16px}.nav{grid-template-columns:repeat(3,1fr)}.metrics{grid-template-columns:repeat(2,1fr)}.topbar{display:block}.search{margin-top:16px}.grid{grid-template-columns:1fr}}@media(max-width:560px){.nav{grid-template-columns:repeat(2,1fr)}.metrics{grid-template-columns:1fr 1fr}.topbar h2{font-size:25px}}
</style>
</head>
<body>
<div class="app">
<aside class="sidebar">
  <div class="brand"><div class="eyebrow">Company brain · offline archive</div><h1>FitLife Brands</h1><p>Web crawl and source explorer</p></div>
  <nav class="nav" aria-label="Primary navigation">
    <button class="active" data-section="overview">Overview</button>
    <button data-section="crawl">Web crawl</button>
    <button data-section="social">Social discovery</button>
    <button data-section="tools">Tool collectors</button>
    <button data-section="documents">Documents</button>
    <button data-section="provenance">Provenance</button>
  </nav>
  <div class="sidebar-foot">Source run <span id="source-run"></span></div>
</aside>
<main class="main">
  <div class="topbar"><div><div class="eyebrow">Evidence workspace · GitHub Pages safe</div><h2 id="page-title">Overview</h2><p id="page-subtitle">Everything collected for fitlifebrands.com, preserved in one searchable artifact and ready to model an agent runtime.</p></div><input id="globalSearch" class="search" type="search" placeholder="Search all paths and content…" aria-label="Search all paths and content"></div>
  <section id="section-overview" class="section active"></section>
  <section id="section-crawl" class="section"></section>
  <section id="section-social" class="section"></section>
  <section id="section-tools" class="section"></section>
  <section id="section-documents" class="section"></section>
  <section id="section-provenance" class="section"></section>
</main></div>
<script id="brain-data" type="application/json">__BRAIN_DATA__</script>
<script>
const brain = JSON.parse(document.getElementById('brain-data').textContent);
const files = brain.files || [];
const byPath = Object.fromEntries(files.map(file => [file.path, file]));
const esc = value => String(value ?? '').replace(/[&<>"']/g, ch => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
const json = value => esc(JSON.stringify(value, null, 2));
const textContent = file => typeof file?.content === 'string' ? file.content : JSON.stringify(file?.content ?? '', null, 2);
const rootFor = path => path.split('/')[0];
const extFor = path => (path.split('.').pop() || '').toLowerCase();
const fileLabel = file => file.path.split('/').pop();
const contentObject = path => byPath[path]?.content || {};
const card = (title, body, count='') => '<article class="card"><h4>' + title + (count ? '<small>' + count + '</small>' : '') + '</h4>' + body + '</article>';
const pre = value => '<pre>' + esc(typeof value === 'string' ? value : JSON.stringify(value, null, 2)) + '</pre>';
function status(value){ const cls = String(value || '').toLowerCase(); return '<span class="status ' + cls + '">' + esc(value || 'unknown') + '</span>'; }
function renderMarkdown(value){ return esc(value).replace(/^### (.+)$/gm,'<h3>$1</h3>').replace(/^## (.+)$/gm,'<h2>$1</h2>').replace(/^# (.+)$/gm,'<h1>$1</h1>').replace(/\*\*(.+?)\*\*/g,'<strong>$1</strong>').replace(/\n\n/g,'</p><p>').replace(/^(.+)$/gm,'<p>$1</p>'); }
function parseCsv(value){ const rows=[]; let row=[], cell='', quote=false; for(let i=0;i<value.length;i++){ const ch=value[i], next=value[i+1]; if(ch==='"' && quote && next==='"'){cell+='"';i++;} else if(ch==='"'){quote=!quote;} else if(ch===',' && !quote){row.push(cell);cell='';} else if((ch==='\n'||ch==='\r')&&!quote){if(ch==='\r'&&next==='\n')i++;row.push(cell);if(row.some(Boolean))rows.push(row);row=[];cell='';} else cell+=ch;} if(cell||row.length){row.push(cell);rows.push(row);} return rows; }
function renderCsv(value){ const rows=parseCsv(value); if(!rows.length)return '<div class="empty">No tabular rows found.</div>'; return '<div class="table-wrap"><table class="table"><thead><tr>'+rows[0].map(cell=>'<th>'+esc(cell)+'</th>').join('')+'</tr></thead><tbody>'+rows.slice(1).map(row=>'<tr>'+rows[0].map((_,i)=>'<td>'+esc(row[i]||'')+'</td>').join('')+'</tr>').join('')+'</tbody></table></div>'; }
function renderFile(file){ if(!file)return '<div class="empty">File not found.</div>'; const ext=extFor(file.path), value=textContent(file); let rendered=pre(value); if(ext==='md')rendered='<div class="detail markdown"><div>'+renderMarkdown(value)+'</div><details><summary>Raw Markdown</summary>'+pre(value)+'</details></div>'; else if(ext==='csv')rendered='<div class="detail">'+renderCsv(value)+'<details><summary>Raw CSV</summary>'+pre(value)+'</details></div>'; else if(ext==='json')rendered='<div class="detail">'+pre(value)+'</div>'; return '<div class="detail"><div class="chips"><span class="chip">'+esc(file.content_type)+'</span><span class="chip">'+esc(file.size_bytes)+' bytes</span><span class="chip">sha256 '+esc(file.sha256)+'</span></div>'+rendered+'</div>'; }
function renderOverview(){ const crawl=contentObject('web_crawl/website_context.json'), quality=contentObject('web_crawl/website_crawl_quality.json'), social=contentObject('social_discovery/social_profiles.json'), collector=contentObject('tools/profile.json'), ext={}; files.forEach(file=>{const e=extFor(file.path)||'none';ext[e]=(ext[e]||0)+1}); document.getElementById('section-overview').innerHTML='<div class="hero"><div class="eyebrow">'+esc(brain.kind)+'</div><h3>'+esc(crawl.summary || 'Company evidence archive')+'</h3><p>Generated '+esc(brain.generated_at)+' · '+esc(brain.purpose)+'</p><div class="metrics"><div class="metric"><strong>'+files.length+'</strong><span>embedded files</span></div><div class="metric"><strong>'+esc(crawl.pages_crawled)+'</strong><span>web pages crawled</span></div><div class="metric"><strong>'+esc(social.profiles?.length || 0)+'</strong><span>social profiles</span></div><div class="metric"><strong>'+esc(collector.collector_status?.length || 0)+'</strong><span>collectors reported</span></div></div></div><div class="grid">'+card('Crawl quality','<div class="chips"><span class="chip">'+esc(crawl.attempted_urls)+' attempted</span><span class="chip">'+esc(crawl.failed_fetches)+' failed</span><span class="chip">'+esc(quality.stop_reason)+' stop</span><span class="chip">SSL fallback: '+esc(crawl.insecure_ssl_fallback_used)+'</span></div>')+card('File formats', '<div class="chips">'+Object.entries(ext).sort((a,b)=>b[1]-a[1]).map(([key,value])=>'<span class="chip">.'+esc(key)+' · '+value+'</span>').join('')+'</div>')+card('Website learnings', renderMarkdown(textContent(byPath['web_crawl/website_learnings.md'])))+card('Integrity note','<p class="muted">All records remain available in their source sections. Search includes paths and full embedded content.</p>')+'</div>'; }
function renderCrawl(){ const context=contentObject('web_crawl/website_context.json'), corpus=contentObject('web_crawl/website_corpus.json'), quality=contentObject('web_crawl/website_crawl_quality.json'), raw=contentObject('web_tools_raw/website_crawl.json'); document.getElementById('section-crawl').innerHTML='<div class="grid">'+card('Crawl context',pre(context))+card('Technical crawl output',pre(raw))+card('Page selection',pre(quality))+card('Corpus metadata',pre({format:corpus.format,raw_html_saved:corpus.raw_html_saved,crawl_quality:corpus.crawl_quality}))+'</div><div class="card" style="margin-top:16px"><h4>Pages crawled <small>'+((context.page_documents||[]).length)+'</small></h4><div class="doc-list">'+(context.page_documents||[]).map((page,i)=>'<div class="doc"><div><strong>'+esc(page.title)+'</strong><small>'+esc(page.url)+' · '+esc(page.text_length)+' chars · '+esc(page.page_type)+'</small></div><button data-page-index="'+i+'">Open page</button></div>').join('')+'</div><div id="crawl-detail"></div></div>'; document.querySelectorAll('[data-page-index]').forEach(button=>button.addEventListener('click',()=>{const page=context.page_documents[Number(button.dataset.pageIndex)];document.getElementById('crawl-detail').innerHTML='<div class="detail">'+pre(page)+'</div>';})); }
function renderSocial(){ const profiles=contentObject('social_discovery/social_profiles.json'), posts=contentObject('social_discovery/x_posts.json'), bundle=contentObject('social_discovery/source_bundle.json'); document.getElementById('section-social').innerHTML='<div class="grid">'+card('Discovered profiles','<div class="doc-list">'+(profiles.profiles||[]).map(p=>'<div class="doc"><div><strong>'+esc(p.platform)+' · @'+esc(p.handle)+'</strong><small><a href="'+esc(p.url)+'" target="_blank" rel="noreferrer">'+esc(p.url)+'</a> · confidence '+esc(p.confidence)+' · ingestion '+esc(p.ingestion_enabled)+'</small></div>'+status(p.source)+'</div>').join('')+'</div>')+card('X retrieval',pre(posts))+card('Diagnostics',pre(profiles.diagnostics))+card('Source bundle',pre(bundle))+'</div>'; }
function renderTools(){ const profile=contentObject('tools/profile.json'), raw=Object.entries(profile.raw_outputs||{}); document.getElementById('section-tools').innerHTML='<div class="grid">'+card('Collector status','<div class="table-wrap"><table class="table"><thead><tr><th>Collector</th><th>Status</th><th>Latency</th><th>Reason</th></tr></thead><tbody>'+(profile.collector_status||[]).map(item=>'<tr><td><strong>'+esc(item.name)+'</strong><br><small class="muted">deps: '+esc((item.dependencies||[]).join(', ')||'none')+'</small></td><td>'+status(item.status)+'</td><td>'+esc(item.latency_ms)+' ms</td><td>'+esc(item.reason||item.error||'')+'</td></tr>').join('')+'</tbody></table></div>')+card('Identity and market',pre({identity:profile.identity,company_context:profile.company_context,market:profile.market,visibility:profile.visibility}))+card('Website health and presence',pre({website_health:profile.website_health,website_presence:profile.website_presence,reputation:profile.reputation}))+card('Recommendations', '<div class="stack">'+(profile.recommendations||[]).map(item=>'<div class="'+(item.severity==='high'?'danger':'warning')+'"><strong>'+esc(item.issue)+'</strong><br><span class="muted">'+esc(item.severity)+' · '+esc(item.confidence)+' · '+esc(item.source_collector)+'</span><br>'+esc(item.evidence)+'</div>').join('')+'</div>')+card('Raw collector outputs','<div class="doc-list">'+raw.map(([key,value])=>'<details><summary>'+esc(key)+'</summary>'+pre(value)+'</details>').join('')+'</div>')+'</div>'; }
function renderDocuments(query=''){ const section=document.getElementById('section-documents'); const all=files.filter(file=>file.path.startsWith('generated_documents/')); const ext=new Set(all.map(file=>extFor(file.path))); const selected=(document.getElementById('doc-type')||{}).value||''; const filtered=all.filter(file=>(!selected||extFor(file.path)===selected)&&(!query||[file.path,textContent(file)].join(' ').toLowerCase().includes(query.toLowerCase()))); section.innerHTML='<div class="card"><h4>Document library <small>'+filtered.length+' of '+all.length+'</small></h4><div class="toolbar"><input id="doc-search" type="search" placeholder="Filter documents…" value="'+esc(query)+'"><select id="doc-type"><option value="">All formats</option>'+Array.from(ext).sort().map(e=>'<option value="'+esc(e)+'"'+(e===selected?' selected':'')+'>'+esc(e.toUpperCase())+'</option>').join('')+'</select></div><div class="doc-list">'+(filtered.length?filtered.map((file,index)=>'<div class="doc"><div><strong>'+esc(fileLabel(file))+'</strong><small>'+esc(file.path)+' · '+esc(file.size_bytes)+' bytes</small></div><button data-doc-index="'+index+'">Open</button></div>').join(''):'<div class="empty">No documents match this filter.</div>')+'</div><div id="doc-detail"></div><p class="footer-note">Markdown, email, CSV, calendar, HTML, PDF, and PPTX payloads are preserved; viewers use safe escaped or structured representations.</p></div>'; document.getElementById('doc-search').addEventListener('input',event=>renderDocuments(event.target.value)); document.getElementById('doc-type').addEventListener('change',()=>renderDocuments(document.getElementById('doc-search').value)); document.querySelectorAll('[data-doc-index]').forEach(button=>button.addEventListener('click',()=>{const file=filtered[Number(button.dataset.docIndex)];document.getElementById('doc-detail').innerHTML=renderFile(file);})); }
function renderProvenance(){ const sourceFiles=files.filter(file=>!file.path.startsWith('generated_documents/')); document.getElementById('section-provenance').innerHTML='<div class="grid">'+card('Run provenance',pre({contract_version:brain.contract_version,kind:brain.kind,workspace_id:brain.workspace_id,generated_at:brain.generated_at,purpose:brain.purpose,provenance:brain.provenance}))+card('Complete manifest','<div class="table-wrap"><table class="table"><thead><tr><th>Path</th><th>Type</th><th>Bytes</th><th>Hash</th></tr></thead><tbody>'+files.map(file=>'<tr><td>'+esc(file.path)+'</td><td>'+esc(file.content_type)+'</td><td>'+esc(file.size_bytes)+'</td><td><code>'+esc(file.sha256)+'</code></td></tr>').join('')+'</tbody></table></div>')+card('Source-file contents <small>'+sourceFiles.length+' files</small>','<div class="doc-list">'+sourceFiles.map((file,index)=>'<div class="doc"><div><strong>'+esc(file.path)+'</strong><small>'+esc(file.content_type)+' · '+esc(file.size_bytes)+' bytes</small></div><button data-source-index="'+index+'">Open source</button></div>').join('')+'</div><div id="source-detail"></div>')+'</div>'; document.querySelectorAll('[data-source-index]').forEach(button=>button.addEventListener('click',()=>{document.getElementById('source-detail').innerHTML=renderFile(sourceFiles[Number(button.dataset.sourceIndex)]);})); }
function verifyCompleteness(){ const roots=['web_crawl/','social_discovery/','tools/','web_tools_raw/','generated_documents/']; return {files:files.length,roots:roots.map(root=>[root,files.filter(file=>file.path.startsWith(root)).length]),documents:files.filter(file=>file.path.startsWith('generated_documents/')).length}; }
function globalSearch(query){ if(!query){renderDocuments('');return;} const matches=files.filter(file=>[file.path,textContent(file)].join(' ').toLowerCase().includes(query.toLowerCase())).slice(0,80); document.getElementById('section-documents').innerHTML='<div class="card"><h4>Global search <small>'+matches.length+' shown</small></h4><div class="doc-list">'+(matches.length?matches.map((file,index)=>'<div class="doc"><div><strong>'+esc(file.path)+'</strong><small>'+esc(file.content_type)+' · '+esc(file.size_bytes)+' bytes</small></div><button data-search-index="'+index+'">Open</button></div>').join(''):'<div class="empty">No records match “'+esc(query)+'”.</div>')+'</div><div id="doc-detail"></div></div>';document.querySelectorAll('[data-search-index]').forEach(button=>button.addEventListener('click',()=>{document.getElementById('doc-detail').innerHTML=renderFile(matches[Number(button.dataset.searchIndex)]);})); }
function activate(name){document.querySelectorAll('.section').forEach(section=>section.classList.toggle('active',section.id==='section-'+name));document.querySelectorAll('.nav button').forEach(button=>button.classList.toggle('active',button.dataset.section===name));document.getElementById('page-title').textContent=name==='crawl'?'Web crawl':name==='social'?'Social discovery':name==='tools'?'Tool collectors':name==='documents'?'Documents':name==='provenance'?'Provenance':'Overview';if(name==='overview')renderOverview();if(name==='crawl')renderCrawl();if(name==='social')renderSocial();if(name==='tools')renderTools();if(name==='documents')renderDocuments();if(name==='provenance')renderProvenance();}
document.querySelectorAll('.nav button').forEach(button=>button.addEventListener('click',()=>activate(button.dataset.section)));
document.getElementById('globalSearch').addEventListener('input',event=>{if(event.target.value){activate('documents');globalSearch(event.target.value);}else activate('overview');});
document.getElementById('source-run').textContent=brain.provenance?.source_run_id || 'n/a';
renderOverview();
</script></body></html>`;

fs.writeFileSync(path.join(outputDir, 'index.html'), html.replace('__BRAIN_DATA__', safeJson));
fs.writeFileSync(path.join(outputDir, 'dependencies.json'), JSON.stringify(['brain_deliverables.json'], null, 2) + '\n');
console.log('Built output/index.html with ' + source.files.length + ' embedded files.');
